{% extends 'base.html.twig' %}

{% block title %}New Course{% endblock %}

{% block body %}
    <!--================Home Banner Area =================-->
    <section class="banner_area">
        <div class="banner_inner d-flex align-items-center">
            <div class="overlay"></div>
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-6">
                        <div class="banner_content text-center">
                            <h2>Новый курс</h2>
                            <div class="page_link">
                                <a href="{{ path('index') }}">Главная</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!--================End Home Banner Area =================-->
    <section class="contact_area section_gap">
        <div class="container">
            <div class="row">
                <div class="col-lg-3">
                    Размещение курсов на Edustage бесплатное, если они открыты для всех желающих.
                </div>
                <div class="col-lg-9">
                    {{ form_start(courseForm, { 'attr': {'class': 'contact_form'} }) }}
                    <div class="form-group">
                        {{ form_row(courseForm.name) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(courseForm.description) }}
                    </div>

                    <div class="custom-control custom-radio">
                        <input type="radio" class="custom-control-input" id="paid"
                               name="accessType">
                        <label class="custom-control-label" for="paid">Платный курс</label>
                    </div>
                    <div class="custom-control custom-radio">
                        <input type="radio" class="custom-control-input" id="free" checked
                               name="accessType">
                        <label class="custom-control-label" for="free">Бесплатный курс</label>
                    </div>
                    <div class="form-group">
                        {{ form_row(courseForm.cost) }}
                    </div>
                    <button class="btn primary-btn">Создать</button>
                    {{ form_end(courseForm) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
